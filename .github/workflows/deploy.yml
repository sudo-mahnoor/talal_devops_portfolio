name: Deploy Portfolio

on:
  push:
    branches: [ staging ]
  pull_request:
    branches: [ staging ]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:

      # 1. Checkout staging branch
      - name: Checkout staging code
        uses: actions/checkout@v4
        with:
          ref: staging
          fetch-depth: 0  # Needed to access full history for merge

      # 2. Show branch info for debugging
      - name: Debug Git Info
        run: |
          git branch -a
          git log --oneline -5

      # 3. Configure Git for merge
      - name: Configure Git
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"

      # 4. Overwrite main with staging
      - name: Overwrite main with staging
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          git remote set-url origin https://x-access-token:${GITHUB_TOKEN}@github.com/${{ github.repository }}.git
          git fetch origin main
          git checkout main
          git reset --hard staging
          git push origin main --force

      # 5. Setup SSH for deployment
      - name: Setup SSH
        run: |
          mkdir -p ~/.ssh
          # Write private key from GitHub Secrets
          echo "${{ secrets.SSH_PRIVATE_KEY }}" | tr -d '\r' > ~/.ssh/github.id
          chmod 600 ~/.ssh/github.id
          # Add server to known_hosts to avoid host key verification issues
          ssh-keyscan -H 13.62.239.109 >> ~/.ssh/known_hosts

      # 6. Deploy main branch to server
      - name: Deploy & Restart Nginx
        run: |
          ssh -tt -i ~/.ssh/github.id -o StrictHostKeyChecking=no deploy@13.62.239.109 << 'EOF'
            set -e
            echo "Deploying latest portfolio..."
            cd /var/www/html/talal_devops_portfolio
            git fetch origin main
            git reset --hard origin/main
            git clean -fdx

            echo "Restarting Nginx..."
            sudo systemctl restart nginx

            sudo systemctl is-active --quiet nginx || {
              echo "Nginx failed!" >&2
              exit 1
            }

            echo "Deployment successful!"
          EOF
